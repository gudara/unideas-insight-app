import { Report } from "@/lib/interfaces/report-interface";

interface ListReportProps {
    list: Report[];
    selectedList?: Report[];
}

export const ListReport: React.FC<ListReportProps> = ({ list, selectedList }) => {
    return <>

    </>
}