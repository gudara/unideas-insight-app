import { LoaderCircle } from "lucide-react";

export default function LoaderComponent() {
  // You can add any UI inside Loading, including a Skeleton.
  return <><LoaderCircle className="animate-spin mx-auto text-gray-400" /></>
}